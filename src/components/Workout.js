import React from "react";

const Workout = (props) => {
  return (
    <div>
      {/* {props.wokout.map((workout) => (
        <li key={workout.id}>
          {workout.name} - {workout.category}
        </li>
      ))} */}
    </div>
  );
};

export default Workout;
